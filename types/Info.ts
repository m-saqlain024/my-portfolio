// Define a type for the User object
type User = {
    name: string;
    education: string;
    degree: string;
};

// Define the array of User objects
export const info: User[] = [
    {
        name: "muhammad saqlain",
        education: "bs in computer sciences",
        degree: "software engineering"
    }
];

// Export the info array
export default info;
